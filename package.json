{
  "name": "code-sense",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    
    "// COMMENT": "👇 THIS LINE IS THE FIX. It builds grammar BEFORE the website.",
    "build": "npm run build:grammar && vite build",
    
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
   "build:grammar": "node -e \"require('fs').mkdirSync('src/grammar', { recursive: true })\" && npx peggy --format es --output src/grammar/cppParser.js src/grammar/cpp.pegjs"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "dagre": "^0.8.5",
    "peggy": "^5.0.6",
    "reactflow": "^11.11.4"
  },
  "devDependencies": {
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@types/dagre": "^0.7.53",
    "@types/node": "^20.11.24",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}